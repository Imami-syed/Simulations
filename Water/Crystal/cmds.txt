>> input initial files will be pdb , em.mdp, npt nvt .mdp 
>> The crystal.pdb file is generated by 
writing and making changes to the residue and the 
file as the tip4p one such that it matches the tip4p 
structure
>> now the gro file is generated by using the commands
    >> gmx editconf -f crystal.pdb -o crystal.gro
    >> gmx genbox -cp crystal.gro -cs spc216.gro -o crystal_solv.gro -p
>> topol file can be found by 2 types
    >> generating the topol file using pdb2gmx command
    >>  gmx pdb2gmx -f crystal.pdb -o crystal.gro -p topol.top
    >> or writing it from the scratch 
    >> link to the website to write this from scratch
    >>> error with the generated topology file in both things:
        ERROR 1 [file topol.top, line 8]:
        ERROR: The cut-off length is longer than half the shortest box vector or
        longer than the smallest box diagonal element. Increase the box size or
        decrease rlist.
    >> to solve this error 
        gmx editconf -f crystal.gro -o crystal.gro -c -box 10 10 10
    >> https://gaseri.org/en/tutorials/gromacs/1-tip4pew-water/#topology-file
>> the mdp file is generated by using the command
    >> https://gaseri.org/en/tutorials/gromacs/1-tip4pew-water/#topology-file
    >> from the above link the files can be generated 
>> em.mdp files are taken and then the commands are used
    >> gmx grompp -f em.mdp -o em.tpr -pp em.top -po em.mdp -c crystal.gro
    >> gmx mdrun -s em.tpr -o em.trr -x em.xtc -c em.gro -e em.edr -g em.log
>> nvt.mdp files are taken and the commands are used 
    >> gmx grompp -f nvt.mdp -o nvt.tpr -pp nvt.top -po nvt.mdp -c em.gro
    >> gmx mdrun -s nvt.tpr -o nvt.trr -x nvt.xtc -c nvt.gro -e nvt.edr -g nvt.log
>> npt.mdp files are taken and the commands are used 
    >> gmx grompp -f npt.mdp -o npt.tpr -pp npt.top -po npt.mdp -c nvt.gro
    >> gmx mdrun -s npt.tpr -o npt.trr -x npt.xtc -c npt.gro -e npt.edr -g npt.log
>> npt.mdp : changes are made 
    >> dt                       = 0.002     ; 2 fs
    >> nsteps                   = 10000    ; 0.1 ns
    >> nstenergy                = 100
    >> nstlog                   = 100
    >> nstxout-compressed       = 100
    >> tau-t                    = 1
    >> ref-t                    = 200
>> this changes are made such that 100 frames in npt.xtc are generated
